{
  "name": "schema-ui-sandbox",
  "version": "1.0.0",
  "private": true,
  "description": "Schema UI Sandbox - A pnpm workspace with Next.js app and shared packages",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "start": "pnpm -r start",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "typegen": "pnpm -r typegen",
    "clean": "pnpm -r clean",
    "install:all": "pnpm install"
  },
  "keywords": [
    "next.js",
    "sanity",
    "workspace",
    "monorepo"
  ],
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
